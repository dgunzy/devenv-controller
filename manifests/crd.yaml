apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: devenvironments.devenv.example.com
spec:
  group: devenv.example.com
  names:
    categories: []
    kind: DevEnvironment
    plural: devenvironments
    shortNames: []
    singular: devenvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for DevEnvironmentSpec via `CustomResource`
        properties:
          spec:
            description: The spec for a DevEnvironment - what the user wants
            properties:
              database:
                description: Optional database type
                nullable: true
                type: string
              image:
                description: Container image to deploy
                type: string
              replicas:
                default: 1
                description: Number of replicas
                format: int32
                type: integer
            required:
            - image
            type: object
          status:
            description: The status of a DevEnvironment - what actually exists
            nullable: true
            properties:
              endpoints:
                description: URLs where the environment can be accessed
                items:
                  type: string
                nullable: true
                type: array
              message:
                description: Human-readable message about current state
                nullable: true
                type: string
              phase:
                description: Current phase (Pending, Creating, Ready, Failed)
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: DevEnvironment
        type: object
    served: true
    storage: true
    subresources:
      status: {}
